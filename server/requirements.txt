# 服务自身依赖（PyTorch 与 OmniVoice 由 Dockerfile 单独安装）
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9
# 限定 4.x / 5.x：Web 页面使用的 mount_gradio_app、Audio(type="filepath") 等
# 接口在该范围内稳定，避免后续大版本变更导致页面不可用
gradio>=4.0.0,<6.0
